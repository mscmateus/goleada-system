package com.goleada.goleada.models.enums;

/*
 * Enum usado para determinar o tipo de codigo de confirmação
 */
public enum ConfirmationType {
   EMAIL_CONFIRMATION, PASSWORD_RESET, EMAIL_ALTERATION
}
